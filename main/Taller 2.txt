class Auto:
    def __init__(self,modelo,precio,marca,motor,registro1,cantidadcreados):
        self.modelo = modelo
        self.precio = precio
        self.motor = motor
        self.registro1 = registro1
        self.cantidadcreados = cantidadcreados
        self.asientos = 0

        def __init__ (self,cantidadasientos):
            self.asientos += cantidadasientos

        def __init__ (self,verificarintegridad) :
            if (self.registro1 == registro2 == registro3
                    print (" Auto original")
                else :
                    print ("Auto pirata")
                    return (verificarintegridad)


                        
class Asientos :
    def __init__ (self,precio,registro2):
        self.color = str
        self.precio = Auto.precio
        self.registro2 = registro2

    def __init__ (self,cambiocolor) :
       self.cambiocolor = cambiocolor
       if cambiocolor = "rojo" or "verde" or "amarillo" or "negro" or "blanco" :
           self.color = cambiocolor

class Motor :

    def __init__ (self,numerocilindros,tipo,registro3) :
      self.numerocilindros = numerocilindros
      self.tipo = str
      self.refistro3 = registro3

    def __init__ (self,cambioregistro):
        self.cambioregistro = registro3

    def __init__ (self,asignartipo):
        self.asignartipo = asignartipo
        if asignartipo = "electrico" or "combustible" :
            self.tipo = asignartipo
    
        
       



    





 

      

   
   


                
