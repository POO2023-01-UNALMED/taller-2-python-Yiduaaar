class Auto:
    def _init_(self,modelo,precio,marca,motor,registro1,cantidadcreados):
        self.modelo = modelo
        self.precio = precio
        self.motor = motor
        self.registro1 = registro1
        self.cantidadcreados = cantidadcreados
        self.asientos = 0

        def _init_ (self,cantidadasientos):
            self.asientos += cantidadasientos

        def _init_ (self,verificarintegridad) :
            if (self.registro1 == registro2 == registro3
                    print (" Auto original")
                else :
                    print ("Auto pirata")


                        
class Asientos :
    def _init_ (self,precio,registro2):
        self.color = str
        self.precio = Auto.precio
        self.registro2 = registro2

    def _init_ (self,cambiocolor) :
       self.cambiocolor = cambiocolor
       if cambiocolor = "rojo" or "verde" or "amarillo" or "negro" or "blanco" :
           self.color = cambiocolor

class Motor :

    def _init (self,numerocilindros,tipo,registro3) :
      self.numerocilindros = numerocilindros
      self.tipo = str
      self.refistro3 = registro3

    def _init_ (self,cambioregistro):
        self.cambioregistro = registro3

    def _iniit_ (self,asignartipo):
        self.asignartipo = asignartipo
        if asignartipo = "electrico" or "combustible" :
            self.tipo = asignartipo
    
        
       



    





 

      

   
   


                
